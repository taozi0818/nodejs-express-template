const path = require('path');

module.exports = {
  'config': path.resolve('config', 'migration.json'),
  'models-path': path.resolve('api', 'models'),
  'migrations-path': path.resolve('db', 'migrations')
}
